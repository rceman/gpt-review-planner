{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gpt-review-planner.invalid/schemas/airelay-session-launch-recipe.schema.json",
  "title": "Airelay session launch recipe",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "recipe_sha256",
    "session_key",
    "profile",
    "working_directory",
    "executable",
    "child",
    "resume",
    "approved_flags",
    "environment_references"
  ],
  "properties": {
    "schema_version": {"const": 1},
    "recipe_sha256": {"$ref": "#/$defs/sha256"},
    "session_key": {"$ref": "#/$defs/sessionKey"},
    "profile": {"$ref": "#/$defs/identifier"},
    "working_directory": {"$ref": "#/$defs/absolutePath"},
    "executable": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "version", "controller_protocol_version"],
      "properties": {
        "path": {"$ref": "#/$defs/absolutePath"},
        "version": {"$ref": "#/$defs/semver"},
        "controller_protocol_version": {"$ref": "#/$defs/semver"}
      }
    },
    "child": {
      "type": "object",
      "additionalProperties": false,
      "required": ["program", "argv"],
      "properties": {
        "program": {"$ref": "#/$defs/absolutePath"},
        "argv": {"$ref": "#/$defs/argv"}
      }
    },
    "resume": {
      "type": "object",
      "additionalProperties": false,
      "required": ["identity"],
      "properties": {
        "identity": {"$ref": "#/$defs/identifier"}
      }
    },
    "approved_flags": {
      "type": "array",
      "uniqueItems": true,
      "maxItems": 64,
      "items": {"$ref": "#/$defs/flag"}
    },
    "environment_references": {
      "type": "array",
      "uniqueItems": true,
      "maxItems": 64,
      "items": {"$ref": "#/$defs/environmentReference"}
    }
  },
  "$defs": {
    "sha256": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "identifier": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$", "minLength": 1, "maxLength": 128},
    "sessionKey": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]{0,127}$", "minLength": 1, "maxLength": 128},
    "semver": {"type": "string", "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)(?:-[0-9A-Za-z-]+(?:\\.[0-9A-Za-z-]+)*)?(?:\\+[0-9A-Za-z-]+(?:\\.[0-9A-Za-z-]+)*)?$"},
    "absolutePath": {
      "type": "string",
      "pattern": "^/(?!.*(?:^|/)\\.\\.?(?:/|$))(?!.*//)(?!.*[\\u0000-\\u001f\\u007f\\\\])[^/]+(?:/[^/]+)*$",
      "minLength": 2,
      "maxLength": 4096
    },
    "shellSafeToken": {
      "type": "string",
      "minLength": 1,
      "maxLength": 1024,
      "pattern": "^(?!.*[\\u0000-\\u001f\\u007f;|&$`()<>]).+$"
    },
    "argv": {
      "type": "array",
      "minItems": 1,
      "maxItems": 128,
      "uniqueItems": true,
      "items": {"$ref": "#/$defs/shellSafeToken"}
    },
    "flag": {"type": "string", "pattern": "^--[A-Za-z0-9][A-Za-z0-9-]{0,63}$"},
    "environmentReference": {"type": "string", "pattern": "^[A-Z_][A-Z0-9_]{0,127}$"}
  }
}
