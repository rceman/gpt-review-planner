# Gateway-Compatible Reference Pack

This dependency-free fixture demonstrates the canonical semantic boundary consumed by gateway protocol v2. It is a validator fixture, not a transport bundle. The gateway owns task routing and archive materialization.
