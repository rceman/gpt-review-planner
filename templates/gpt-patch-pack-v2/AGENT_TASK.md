# AGENT_TASK

Apply the data-only GPT Patch Pack v2 payload with the pinned v2 runner.

The declared execution mode is authoritative. In `gpt_tunnel_managed` mode,
write only the canonical tunnel completion and use the gateway finalize path;
do not create repository evidence or an evidence-only commit.
