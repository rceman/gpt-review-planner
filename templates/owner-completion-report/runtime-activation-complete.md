# Runtime activation complete

Outcome: Activated `<plain-language runtime result>`; state: `activated`.

## What changed

- Installed version: `<independently established version>`
- Running version: `<independently established version>`
- Activation: `<independently established result>`
- Connector refresh: `<independently established result or None>`

### New tools

Choose exactly one mutually exclusive form: one or more
`- <tool>: <plain-language behavior/impact>` entries, or exactly `- None`.
Delete the unused alternative before delivery; never retain both.

### Updated tools

Choose exactly one mutually exclusive form: one or more
`- <tool>: <plain-language behavior/impact>` entries, or exactly `- None`.
Delete the unused alternative before delivery; never retain both.

### Removed tools

Choose exactly one mutually exclusive form: one or more
`- <tool>: <plain-language behavior/impact>` entries, or exactly `- None`.
Delete the unused alternative before delivery; never retain both.

## Why it matters

<Why the independently proven runtime state matters>.

## Availability

- Usable now: installed, running, and activated only where each is evidenced.
- Still unavailable or unproven: `<remaining state or None>`.

## Next action

<One exact operational decision or authorization>

or: `None — no owner action required`

## Technical record

- Final/accepted/source/runtime identity: `<runtime/source identity>`
- Relevant CI/evidence/diagnostic: `<readiness, protocol, or rollback reference>`
- Task/run: `<compact ID when useful or None>`
- Deviations: `<None or concise deviation>`
- Prohibited operations: `<confirmation>`
