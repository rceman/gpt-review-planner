# Merge complete

Outcome: Merged `<plain-language change>` into the authorized source branch; state: `merged`.

## What changed

- `<plain-language merged change>`

## Why it matters

<Why the merged source change matters>.

## Availability

- Usable now: merged source state.
- Still unavailable or unproven: release, installation, running, activation,
  and connector refresh unless each is separately evidenced.

## Next action

<One exact release or owner decision, if authorized>

or: `None — no owner action required`

## Technical record

- Final/accepted/source/runtime identity: `<merge identity and ordered parents>`
- Relevant CI/evidence/diagnostic: `<compact CI or evidence reference>`
- Task/run: `<compact ID when useful or None>`
- Deviations: `<None or concise deviation>`
- Prohibited operations: `<confirmation>`
