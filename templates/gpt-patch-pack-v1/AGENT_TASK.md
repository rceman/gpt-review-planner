# AGENT_TASK

## Compatibility declaration

Compatibility scope: none
Compatibility authorization: not granted
Canonical implementation: <single schema/protocol/path>
Legacy behavior: unsupported and out of scope

Use only the standard GPT Patch Pack v1 runner. Do not manually execute payload
helpers, reconstruct the patch, introduce compatibility behavior, broaden scope,
merge, release, or tag.

If a gate appears to require compatibility behavior, stop with
`BLOCKED_UNAUTHORIZED_COMPATIBILITY_CHANGE`.
